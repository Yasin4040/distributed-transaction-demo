server:
  port: 8086

spring:
  application:
    name: best-effort-notification-demo
  datasource:
    url: jdbc:mysql://localhost:3306/dtx_notification?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
